# AegisNet — Project Status

**Last updated:** 2026-08-30 · **Current phase:** M1 Chunk 1 (foundation) verified locally; CI not yet run · **Version:** none tagged

> This file is the single source of truth for progress. It states only what has **evidence**. Nothing below is
> claimed to run, pass, or exist unless an evidence entry is given.

---

## Current state

| | |
|---|---|
| Phase | **Milestone 5 in progress — Chunk 22 (the hardened Perplexity client, the brief schema, citation and safety checks, ADR-030) verified locally (E-72); CI evidence (E-73) pending the push. Still **off by default and no call has been made**. Chunk 23 (revision 0005, the brief routes, the worker and `make brief`) next** |
| Application code written | Settings, JSON logging, error envelope, `/healthz`, `/readyz`, `/api/v1/meta/version`, DB/Redis connectivity adapters, Dramatiq broker with zero actors; Alembic baseline `0001_m1_baseline` for the nine M1 tables, ORM models, schema enums in `domain/`, `schema_revision()` (ADR-012); EVE domain — limits, sanitiser, schema, canonical hash, normaliser — dataset registry adapter, synthetic generator and committed corpus (ADR-013); ingest service, SQL ingest store, `import_dataset` actor in the `workers` layer, operator CLI (ADR-014); asset inventory with overlap rules and CIDR resolution, event read queries with keyset pagination, revision 0002, `make seed` (ADR-015); Argon2id users, HS256 access tokens, rotating refresh tokens with reuse detection, service tokens, the deny-by-default permission dependency on every route, Redis rate limits and denylist, the bounded audit writer, the upload spool and `import_upload` actor, the HTTP routes for auth, ingest, batches, assets, events and audit, the user/token CLI, `SECURITY.md` (ADR-016); the detector contract (`domain/detectors/`), the severity formula, the registry and D-001 port scan with seven labelled fixtures (ADR-017); revision 0003 with the six detection tables, the detection stores, the sweep service, the `run_detectors` actor, the alerts, rules, runs and sweep routes and the detection CLI (ADR-018); D-002 auth-failure burst and D-003 DNS anomaly with thirteen labelled cases (Chunk 10); D-004 beaconing, D-005 outbound volume against `asset_baselines`, the baseline job, actor, CLI and routes, fourteen more labelled cases (Chunk 11, ADR-019); the `scheduler` service (periodiq) with the `scheduled_sweep` and `nightly_baselines` periodic actors, the post-ingest sweep from the worker and the sync route, the evaluation harness (`eval-detectors`, `make eval`) and the pinned §8 table (Chunk 12, ADR-020); the flow-start event time and the DNS direction fixes with regenerated corpus and fixtures (Chunk 14, ADR-022); the correlation engine (`domain/correlation.py`, `domain/incidents.py`), revision 0004 with the four incident tables and the case-number sequence, the incident store, the correlation service and its CLI commands (Chunk 15, ADR-023); the incident service with the workflow enforced by a compare-and-set, the six incident routes, the `incidents.read`/`incidents.write` permissions and the note and closure-reason validation (Chunk 16, ADR-024); the committed multi-stage scenario with its generator and ground truth, the pairwise correlation metrics and `make demo-scenario` (Chunk 17, ADR-025); the isolated Suricata lab — `infra/lab/` with its compose file, IDS-only sensor configuration, three alert-only rules, the lab target and traffic generator, `tools/sanitize_eve.py`, the committed sanitised capture `samples/lab/lab-capture-01.ndjson` and the lab policy suite (Chunk 13, ADR-021) |
| Repository | **Private** on GitHub (the owner's choice) with Dependabot alerts, secret scanning and push protection enabled; MIT; `CONTRIBUTING.md`, `SECURITY.md`, pull-request template. An earlier revision of this row said *public*; `gh repo view` says otherwise and the repository has been private throughout |
| Frontend | **The analyst dashboard**: sign in and out, the incident queue with filters and keyset paging, and the case view — alerts, timeline, the status control drawn from the API's `allowed_transitions`, notes through the `SafeMarkdown` element renderer, the asset inventory and the admin-only audit viewer (Chunks 18–20, ADR-026 – ADR-028). A zod-parsed API boundary; the session held entirely in the server's own `HttpOnly` cookies. `tsc`, ESLint (with the `dangerouslySetInnerHTML` ban), 99 vitest tests, `next build` and a 14-test Playwright suite against the running stack all run in CI |
| Tests written | 1 127 hermetic (unit, integration, security, detectors) plus 72 database tests (marker `db`, opt-in, real PostgreSQL) |
| Tests run | **Yes, locally** — hermetic, native database, Compose database and stack paths all in E-50 |
| Docker stack | **Built and started locally; all five services healthy** — see evidence E-3 |
| Database | Roles `aegisnet_migrator` / `aegisnet_app` created at init; **nineteen tables created by `make migrate`** (nine from the M1 baseline, six detection tables from revision 0003, four incident tables from revision 0004) under the migrator role, runtime-role privileges proven exact by the database suite (E-18, E-46, E-58); `/api/v1/meta/version` reports the packaged head `0004_incident_tables` |
| Perplexity integration | **A client now exists, it is off by default, and no API call has been made from this repository.** Chunk 21 built the outbound boundary (`domain/redaction/`, ADR-029); Chunk 22 added `adapters/perplexity/` and `domain/briefs/` (ADR-030) — recommendations are an enum, external claims need https citations, uncited ones are marked `UNVERIFIED`, and a brief has no field that could change a case. `BRIEF_ENABLED` defaults to false and a key alone does nothing. Every test runs against committed fixtures through a mock transport; see [`docs/perplexity-integration.md`](perplexity-integration.md) |
| CI | `ci` **green** end to end, including the stack job which now authenticates and ingests over HTTP (E-38; earlier E-10, E-13). `security` failed on real findings on the first push (E-11), fixed by dependency upgrades (E-12), **green** since (E-13). Every job carried a Node 20 deprecation annotation; cleared by moving to Node 24 action releases (E-15) |
| SonarCloud | External GitHub App check (automatic analysis, not a workflow in this repository). **Quality gate passes** since 7dda2b0 (E-41). It had failed on every earlier analysis: two `S7493` bugs and the Blocker behind *Security E* were found with a local SonarQube Community scan (E-39, E-40); the last condition, *Security Rating on New Code C*, was located by bisecting the analysis scope through `.sonarcloud.properties` and turned out to be the `Path.chmod` call in `infra/scripts/bootstrap_env.py` (E-41). The project stays private on sonarcloud.io, so its badge and API are not public |
| Detector accuracy | **Unmeasured. No claims.** |

## Evidence (local, 2026-08-30, Windows 11 host, Docker Desktop 29.7 / Compose v5.4)

| ID | Command | Result |
|---|---|---|
| E-1 | `cd backend && ENV=test uv run pytest` | `124 passed` |
| E-2 | `ENV=test uv run pytest --cov=aegisnet` | 96% line+branch coverage; only `worker.py` (import-time entrypoint) and the real DB/Redis round trips are uncovered |
| E-3 | `make bootstrap && docker compose up -d --wait --wait-timeout 240` | `db`, `redis`, `api`, `worker`, `web` all `(healthy)`; `api` on `127.0.0.1:8000`, `web` on `127.0.0.1:3000`, no other port published |
| E-4 | `curl 127.0.0.1:8000/healthz`, `/readyz`, `/api/v1/meta/version`, `127.0.0.1:3000/api/health` | all `200`; readiness `{"status":"ok"}`; response carries `x-correlation-id` |
| E-5 | `docker compose exec <svc> id` for every service | `db` uid 70, `redis` uid 999, `api`/`worker` uid 10001, `web` uid 1000 — no root |
| E-6 | `docker compose exec worker python -c "...build_broker(get_settings()).client.ping()"` | `True` (worker authenticates to the `--requirepass` Redis); declared actors `[]` |
| E-7 | `psql` as `aegisnet_app`: `CREATE TABLE` | `permission denied for schema public`; the same statement as `aegisnet_migrator` succeeds |
| E-8 | `uv run ruff check src tests`, `ruff format --check`, `mypy` | clean |
| E-9 | `docker compose -f docker-compose.test.yml run --rm --build tests` | `124 passed` inside the non-root `dev` image, with no secret variable set and unresolvable datastore hostnames |
| E-10 | GitHub Actions `ci` run **33331753840** (first push) | ✅ all four jobs, including `stack`: `docker compose up --build --wait` reached healthy on the runner and every published endpoint answered |
| E-11 | GitHub Actions `security` run **33331753793** (first push) | ❌ genuine findings: starlette 0.46.2 carried 9 advisories (via the fastapi pin) and next 14.2.35 carried 10 high (patched in ≥15.5.21, plus bundled postcss 8.4.31) |
| E-12 | After upgrading (fastapi 0.141 → starlette 1.6.0, uvicorn 0.52; next 15.5.24, react 19, postcss override): `uvx pip-audit --strict` on the exported lockfile and `pnpm audit --prod --audit-level=high` | both clean locally; `124 passed` unchanged |
| E-13 | GitHub Actions `security` runs **33332243302** (push, 2026-08-30) and **33399756070** (weekly schedule, 2026-08-31) | ✅ gitleaks, pip-audit and pnpm audit all clean on the runner |
| E-14 | 2026-09-04, macOS host: `uvx pip-audit --strict` on the exported lockfile, `pnpm audit --prod --audit-level=high`, `ruff check`, `ruff format --check`, `mypy`, `ENV=test uv run pytest`, `pnpm typecheck` | all clean against that day's advisory data; `124 passed` |
| E-15 | Annotations on every job of runs 33332243290 (`ci`) and 33399756070 (`security`) | ⚠️ "Node.js 20 is deprecated … forced to run on Node.js 24" for `checkout@v4`, `setup-node@v4`, `upload-artifact@v4`, `setup-uv@v5`, `gitleaks-action@v2`; GitHub removes Node 20 from hosted runners on 2026-09-16. Fixed by moving each to a Node 24 release; the result is recorded on the push that carries it |
| E-16 | Push a8e9510 (Node 24 actions): `security` run **33918434907**, `ci` run **33918434915** | ✅ both green, no Node 20 annotation. One new annotation on the `ci` backend job: "Failed to save: Unable to reserve cache … another job may be creating this cache" — the `security` pip-audit job had saved a 7.9 MiB cache under the shared key first. Fixed by disabling the cache in that job and deleting the stale entry. Verified on push e712429: `security` run **33918817419** and `ci` run **33918817392** both green with no annotation, and the backend job saved a 41 MiB cache (the runtime set) under its key |
| E-17 | SonarCloud Code Analysis check (`sonarqubecloud` app) on 9ef3024, 89f8dae, a8e9510, e712429, fc53775 | ❌ every one "Quality Gate failed"; the only failing condition is **Security Rating on New Code C** (required A). The project is private on sonarcloud.io, the check carries no annotation and no notification e-mail exists, so the exact finding could not be read. The two request-derived flows Sonar's Python taint rules cover are now neutralised at the sink (`untrusted_text` in the unhandled-exception log call; `canonical_correlation_id` before the response header); 135 tests, 96% coverage. Result recorded on the push carrying it |
| E-18 | 2026-09-05, macOS host, native PostgreSQL 16.15 (Homebrew) initialised with `infra/postgres/init/01_roles.sh`: `AEGISNET_DB_TESTS=1 uv run pytest -m db -v` | `19 passed`: the nine tables and nothing else; `alembic_version` equals the packaged head; Alembic `compare_metadata` reports **no** difference between `models.py` and the migrated schema; enum labels; GIST `inet_ops`, GIN `jsonb_path_ops`, partial and DESC index definitions; `event_hash` 32-byte check and uniqueness; case-insensitive `users.email`; server-side defaults; app-role privilege matrix exactly `SELECT, INSERT, UPDATE` on the ordinary tables, `SELECT, INSERT` on `audit_log`, `SELECT` on `alembic_version`; the migrator owns every table; UPDATE/DELETE/TRUNCATE on `audit_log`, DELETE on every table, and CREATE/ALTER/DROP/CREATE EXTENSION all refused; head → base → head round trip leaves only the empty `alembic_version` |
| E-19 | 2026-09-05: `ruff check`, `ruff format --check`, `mypy`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85` | clean; `137 passed, 19 skipped`; coverage 98% (the migration environment is excluded from the hermetic gate and exercised by E-18 instead, ADR-012) |
| E-20 | 2026-09-05, macOS host, Docker Desktop 29.4 / Compose v5.1: `make test-db`, then `make up && make migrate && make migrate-status`, probes, `make down` | ✅ Compose path: `db-test` healthy, `tests-db` `19 passed`, teardown clean. Stack path: all five services healthy; `alembic upgrade head` ran inside the api image as the migrator; `alembic current` and `heads` both `0001_m1_baseline (head)`; `/readyz` `{"status":"ok"}`; `/api/v1/meta/version` carries `"schema_revision":"0001_m1_baseline"`; `\dt` as `aegisnet_app` lists the nine tables plus `alembic_version`, every one owned by `aegisnet_migrator`; `UPDATE audit_log` as `aegisnet_app` → `permission denied for table audit_log`. (Docker Desktop first hung on image pulls for most of the session and needed a forced restart; the native run E-18 preceded this.) |
| E-21 | Push 2d3a437: GitHub Actions `ci` run **33950753099** and `security` run **33950753033** | ✅ all five `ci` jobs green with no annotation — `backend`, `frontend`, `manifests`, **`migrations` (upgrade, grants, downgrade on PostgreSQL 16, 32s)** and **`stack` (compose up --build reaches healthy, migrate, 1m53s)**; `security` green. SonarCloud still reports *Quality Gate failed* on the same single condition (E-17) |
| E-22 | 2026-09-05: `ruff check` (backend and `tools/`), `ruff format --check`, `mypy` (strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85` | clean; both import contracts kept; `262 passed, 19 skipped`; coverage 98%, every `domain/eve` module 100%. `python3 tools/gen_synthetic_eve.py` regenerates the committed corpus byte-identically (sha256 `5f1c7bd2…`, recorded in `samples/registry.yml` and verified by `tests/integration/test_samples_corpus.py`) |
| E-23 | Push cae5d5d (Chunk 3): GitHub Actions `ci` run **33952182281** and `security` run **33952182289** | ✅ all five `ci` jobs green with no annotation, including the backend job's new `lint-imports` step and ruff over `tools/`; `security` green. SonarCloud unchanged: *Quality Gate failed* on the same single condition (E-17) |
| E-24 | 2026-09-05: `ruff check` (backend, `tools/`), `ruff format --check`, `mypy` (43 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85` | clean; both contracts kept (entrypoints over services over adapters over domain); `287 passed, 24 skipped`; coverage 92% (the CLI wiring and the worker actor are exercised by the database suite and the stack, not the hermetic gate) |
| E-25 | 2026-09-05, native PostgreSQL 16.15: `AEGISNET_DB_TESTS=1 uv run pytest -m db -v` | `24 passed`: the committed corpus imports with counts equal to its manifest (2000 stored), a second import stores 0 and reports 2000 duplicates, the batch row carries method/dataset/licence/timestamps, hostile lines persist as rejects with line numbers and reason codes, promoted columns match the normaliser, and `import_dataset` runs end to end through a `StubBroker` against a pre-opened batch |
| E-26 | 2026-09-05, Docker Desktop 29.4 / Compose v5.1: `make test-db` | ✅ `24 passed` inside `tests-db` against `db-test`, teardown clean |
| E-27 | Same day: `make up && make migrate`, then the CLI inside the api image: `import-dataset synthetic-benign-baseline-01` (sync) twice, once more with `--mode async`, `batch <id>` polled, worker log read, `make down` | ✅ first run `stored 2000, duplicate 0`; second run `stored 0, duplicate 2000`; the async run returned `{"status": "received", "message_id": …}` at once and the worker finished it two seconds later (`worker_started … "actors_registered": ["import_dataset"]`, then `ingest_batch_finished` and `import_dataset_done` with `duplicate 2000`); `SELECT count(*) FROM events` as the app role = 2000 across three `complete` batches; stack torn down |
| E-28 | Push b8791bc (Chunk 4): GitHub Actions `ci` run **33953478893** and `security` run | ✅ all five `ci` jobs green with no annotation — `migrations` now includes the ingest store tests and the actor through a `StubBroker` (45s), `stack` migrates and probes (1m46s); `security` green. SonarCloud unchanged: *Quality Gate failed* on the same single condition (E-17) |
| E-29 | 2026-09-05: `ruff check`, `ruff format --check`, `mypy` (50 files), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85` | clean; both contracts kept; `381 passed, 39 skipped`; coverage 94% with the SQL stores and the worker excluded from the hermetic gate (they run in E-30) |
| E-30 | 2026-09-05, native PostgreSQL 16.15: `AEGISNET_DB_TESTS=1 uv run pytest -m db -v` | `39 passed`: revision 0002 applies and downgrades; the grants matrix shows DELETE on `asset_networks` only; asset store create/lookup/atomic bulk/update-replaces-networks/list filters and cursors/resolution precedence proven with owner-inserted overlaps; the committed seed file seeds 14 then updates 14; event read filters (type, CIDR, address, port, flow, batch, asset), a 4-page keyset walk over 13 rows with no gap or repeat, payload only on request, stats by type and hour; batch and reject listing with cursors |
| E-31 | 2026-09-05, Docker Desktop: `make test-db` | ✅ `39 passed` inside `tests-db` against `db-test`, teardown clean |
| E-32 | Same day: `make up && make migrate`, then the CLI inside the api image: `seed-assets lab-assets` twice, `resolve 10.10.0.53` and `resolve 192.0.2.99`, `import-dataset synthetic-benign-baseline-01`, `assets -q resolver`, `events --asset-id <resolver> --type dns --limit 2`, `event-stats`, `batches --limit 1`, the version endpoint, `make down` | ✅ `alembic` applied `0001_m1_baseline` then `0002_asset_network_delete_grant`; seed reported `created 14, updated 0` then `created 0, updated 14`; the resolver address matched `resolver.lab.example.test` (criticality 4) and the documentation address returned `{"matched": false}`; ingest stored 2000; the asset-scoped DNS query returned events whose destination is a lab host with `dns_rcode NOERROR`; stats reported `total 2000` with per-type counts equal to the corpus manifest and two hourly buckets (1256, 744); the version endpoint reported `"schema_revision":"0002_asset_network_delete_grant"`; stack torn down |
| E-33 | Push deb1b58 (Chunk 5): GitHub Actions `ci` run **33954760088** and `security` run | ✅ all five `ci` jobs green with no annotation — `migrations` (39 database tests incl. revision 0002, 1m06s), `stack` (migrate to `0002_asset_network_delete_grant`, probes, 1m59s); `security` green. SonarCloud unchanged: *Quality Gate failed* on the same single condition (E-17) |
| E-34 | 2026-09-05: `ruff check`, `ruff format --check`, `mypy` (64 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85` | clean; both contracts kept; `565 passed, 44 skipped`; coverage 94% with the SQL stores and the worker excluded from the hermetic gate (they run in E-35). New: the auth domain and service (lockout after 5 and release after the window, rotation, reuse revokes the chain, logout denylists the `jti`, forged/tampered/`alg=none`/over-long/future-dated tokens refused, tokens die on role change), the audit writer's bounds, the Redis limiter and denylist on `fakeredis`, the spool, the auth CLI; `tests/security/test_rbac.py` enumerates every `APIRoute` against the four-entry allowlist and runs 19 routes × 4 roles; the auth, ingest, asset, event and audit routes over the fake wiring incl. cookie flags, replayed cookie, `429` + `Retry-After`, fail-open reads and fail-closed login/ingest, and the audit entry each route leaves |
| E-35 | 2026-09-05, native PostgreSQL 16.15: `AEGISNET_DB_TESTS=1 uv run pytest -m db -v` | `44 passed`: everything in E-30 plus the SQL user store (unique e-mail, failure count, lock and release), refresh tokens (rotate, `revoke_chain` reaches only the live descendant), service tokens (unique name, touch, first revocation wins), the audit store (append, newest first, filters, cursor, full round trip incl. address and nested detail), and the auth service end to end on the SQL stores |
| E-36 | 2026-09-05, Docker Desktop 29.4: `make test-db` | ✅ `44 passed` inside `tests-db` against `db-test`, teardown clean |
| E-37 | Same day: `make up && make migrate`, then `make create-user` (password piped to stdin), `make create-service-token`, `make users`, `make service-tokens`, and the HTTP API with `curl`: version without and with a credential, login (wrong password too), `/auth/me`, `make seed`, async ingest of the 2000-line corpus with the service token, poll as the user, the same corpus in `sync` mode, the same corpus async again, batches as the service token, resolve, events, stats, audit, refresh, replayed cookie, rotated cookie, logout, `make revoke-service-token`, `make down` | ✅ `alembic` applied 0001 then 0002; the user and the token were created and listed without hashes; `/api/v1/meta/version` answered `401 unauthenticated` bare and `200` with `X-Ingest-Token`, carrying `"schema_revision":"0002_asset_network_delete_grant"`; login returned a 900 s bearer and `aegisnet_refresh=…; HttpOnly; Max-Age=1209600; Path=/api/v1/auth; SameSite=strict; Secure`; the wrong password got the identical `401 invalid_credentials` body; async ingest answered `202` with a `poll_url` and the worker finished the batch from the shared spool within one poll (`stored 2000`); `mode=sync` on the same file answered `413` ("at most 1000 lines; use mode=async"); the async re-run reported `duplicate 2000, stored 0`; the spool directory was empty afterwards; the service token got `403 forbidden` on batches; `resolve 10.10.0.53` → `resolver.lab.example.test`; the DNS event page carried payloads for the admin and a cursor; stats `total 2000` with the manifest's per-type counts and hourly buckets (1256, 744); the audit trail listed `user.created`, `service_token.created`, `auth.login_success`, `auth.login_failed` (reason `wrong_password`), both `ingest.batch_created`, `ingest.refused` (`sync_lines_exceeded`) and `rbac.denied` newest first; refresh rotated the cookie (`200`), replaying the old cookie got `401` and killed the rotated one too; logout `204` and the bearer then `401`; the revoked service token got `401` on ingest. The first run of this demo failed with `500` on the upload: Compose had created the `ingest_spool` volume root-owned while the api runs as uid 10001. Fixed by creating `/app/spool` owned by the runtime user in the image (a named volume inherits it) and by `Spool.ensure_writable()` at api and worker startup, so the defect now surfaces as a startup failure, never as a `500` |
| E-38 | Push 6d6f245 (Chunk 6) and 271a8a0: GitHub Actions `ci` run **33957820525** and `security` run **33957820508** | ✅ all five `ci` jobs green with no annotation — `backend` (565 hermetic tests, 45s), `migrations` (44 database tests, 72s), `stack` (124s: CLI created the admin and the service token, bare `/api/v1/meta/version` answered `401`, the token got `200` with revision 0002, the 2000-line corpus ingested over HTTP with the service token and the worker completed it from the shared spool, the user read `"stored":2000` and `ingest.batch_created` in the audit trail); `security` green (`pip-audit`, `pnpm audit`, `gitleaks`). The first push failed only `gitleaks`: the literal HS256 test key in four test files matched its generic-api-key rule; 271a8a0 derives the key from a low-entropy expression instead. SonarCloud unchanged: *Quality Gate failed* on the same single condition (E-17) |
| E-39 | 2026-09-05: the SonarCloud check on 271a8a0 reads *E Security Rating on New Code* and *C Reliability Rating on New Code* (4935ea0, before Chunk 6, already read C/C). Local reproduction: `sonarqube:community` 26.9 in Docker plus `sonar-scanner` 8.1 on the host, first with `sonar.sources=.` as automatic analysis does, then with the new `.sonarcloud.properties` | The first scan reported exactly two bugs and no vulnerability: `python:S7493` (synchronous `Path.open()` inside an `async def`) at `services/ingest_service.py:233` and `workers/actors.py:79`, which is the Reliability C. Fixed by reading NDJSON through `anyio` (`adapters/files/ndjson.read_lines`, `Spool.lines`, `Spool.write` on `anyio.open_file`) and letting `IngestService.ingest` take a sync or async line source; the ingest route now mints the spool entry name before reading the body, so nothing derived from an upload reaches a path. The second scan (tests declared as tests) reported 0 bugs, 0 vulnerabilities, 0 hotspots over 6 800 lines. Community edition has no taint analysis, so the Security E cannot be reproduced locally; it is expected to be an injection-family finding that only the SonarCloud analysis can name. After the rework: hermetic suite green (566 tests), native database suite `44 passed`, `make test-db` `44 passed`, and the full E-37 stack demo repeated with the same outcomes (async upload finished by the worker from the shared spool, `stored 2000`, spool empty) |
| E-40 | 2026-09-05: SonarCloud checks on 2103876 (E-39 rework), 3031c76 (no client text in the unhandled-error log: matched route template and a fixed-set method; the echoed `X-Correlation-ID` rebuilt from the parsed UUID's integer) and 037c52d (the integer forced through `int()`) | 2103876: *Reliability Rating on New Code* no longer fails and *Security Rating on New Code* fell from **E to C**, so the Blocker was the upload-bytes → spool-name → path flow removed in E-39. 3031c76 and 037c52d: still **C**. The remaining Major finding therefore is none of: the unhandled-error log fields, the correlation-id header echo, the spool path. It has been C since E-17 (before any ingest or auth code existed) and cannot be reproduced by SonarQube Community (E-39: zero vulnerabilities on the same tree), so it is a SonarCloud-only taint finding whose text is available only on the private dashboard |
| E-41 | 2026-09-05: with the repository public on GitHub the SonarCloud project stayed private, so the last finding was located by bisection: nine `[skip ci]` commits (bb8538b … 7dda2b0) changed `sonar.exclusions` in `.sonarcloud.properties` and the check on each was read from the GitHub API | Excluding the auth and ingest routers: still C. Excluding all of `backend/src`: still C. Analysing `backend/src` only: **passed**, so the properties file is honoured and the finding was outside the backend. Excluding the frontend and the tests: still C. Excluding `infra` and `tools` as well: passed. Keeping `tools`, excluding `infra`: passed. Keeping the PostgreSQL init script, excluding `infra/scripts`: passed. The file was `infra/scripts/bootstrap_env.py`; its only security-relevant calls were `os.open(…, 0o600)` and a follow-up `Path.chmod(stat.S_IRUSR \| stat.S_IWUSR)`, and SonarCloud's public rule catalogue lists exactly one Major Python vulnerability rule that matches such code, `S2612` (world-accessible file permissions). The chmod was redundant on POSIX (the umask can only tighten the 0600 the file is created with) and was removed; with the full scope restored the gate **passed** (7dda2b0). Both GitHub workflows were skipped on the probe commits and run again on the final push |
| E-42 | 2026-09-05, Chunk 7: `ruff check`, `ruff format --check`, `mypy` (65 files), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85`, native database suite | clean; both contracts kept; `566 passed, 44 skipped`, coverage 94 %; `44 passed`. New behaviour: a `dataset_id` that fails its grammar on the import route is audited as `ingest.refused` naming the field and the caller, never the value; an unauthenticated attempt is `401` and leaves no entry. Documents reconciled at the gate: `ARCHITECTURE.md` (status, implementation-status section, M1 topology, Redis failure mode), `THREAT_MODEL.md` (gate review, T-1.6), `docs/evaluation.md`, the delivery plan's seven M1 acceptance boxes with evidence, the last API acceptance box, the DoD checklist and documents table here |
| E-43 | Push 73dba41 (Chunk 7): GitHub Actions `ci` run **33966427477**, `security` run **33966427581**, SonarCloud check | ✅ all five `ci` jobs green with no annotation — `backend` 41s, `migrations` 67s, `stack` 95s (compose up, migrate, CLI user and token, `401` without a credential, HTTP ingest of the corpus, worker completion, audit read); `security` green (`pip-audit`, `pnpm audit`, `gitleaks`); SonarCloud **Quality Gate passed** |
| E-44 | 2026-09-05, Chunk 8: `ruff check` (backend, `tools/`), `ruff format --check`, `mypy` (70 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85`, native database suite, `python3 tools/gen_labelled_fixtures.py` into a temporary directory | clean; both contracts kept (the new package is inside `domain/` and imports nothing from the infrastructure); `604 passed, 44 skipped`, coverage 94.8 %, `domain/detectors/` 97 – 100 % per file; `44 passed`. The 38 new tests cover the window bounds (naive timestamps, events outside the window, spans over 24 h, the 200 000-event cap, sorting), evidence bounds (forbidden keys, long or control-character strings, nesting, list and key caps, NaN), result validation and the dedup key, bucket flooring, the severity formula at its clamps with `reproduce` catching a tampered result, D-001's inclusive thresholds, the backup-client guard (500 flows to one target: nothing), `min_flows`, non-flow events ignored, signal and confidence values, sample roles, purity and order independence, the registry, and all seven labelled cases (three positives fire on the labelled entity at severity ≥ 3, four negatives stay silent); the regenerated fixtures are byte-identical to the committed ones |
| E-45 | Push 4f5c5d5 (Chunk 8): GitHub Actions `ci` run **33967258819**, `security` run **33967258824**, SonarCloud check | ✅ all five `ci` jobs green with no annotation — `backend` 40s (604 hermetic tests incl. the 38 detector tests and the fixture pin), `migrations` 56s, `stack` 101s; `security` green; SonarCloud **Quality Gate passed** |
| E-46 | 2026-09-05, Chunk 9: `ruff check` (backend, `tools/`), `ruff format --check`, `mypy` (76 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85`, native database suite, `make test-db`, then `make up && make migrate` and the E-37 stack demo extended with `POST /api/v1/detections/sweeps`, `/detections/runs`, `/alerts`, `/detections/rules` and `make run-detectors`, `make down` | clean; both contracts kept; `658 passed, 47 skipped`, coverage 95 %; `47 passed` natively and `47 passed` in Compose: revision 0003 applies and downgrades (fifteen tables, fifteen enum types, `compare_metadata` clean), the grants matrix shows SELECT/INSERT/UPDATE on the six new tables and DELETE still only on `asset_networks`, the rule store upserts to the code version and keeps the operator's flag, alerts dedup at the database with their sampled events and linked assets, list filters and cursors, and the sweep runs end to end over the ingested `vertical-40-ports` fixture (one alert at severity 4 from asset criticality 5, rationale reproduces, a re-sweep over an unaligned interval creates nothing, twenty sampled events, one linked asset). Stack: `alembic` applied 0001, 0002 and 0003; the version route reported `0003_detection_tables`; the admin's sweep request over the corpus window answered `202`; within one poll the worker had recorded `D-001 success, events 2000, alerts 0, 2 ms`; `/alerts` answered an empty page (the corpus is benign by construction), `/detections/rules` listed D-001 v1 with its defaults; `make run-detectors` over the labelled window ran inline and printed one `success` run. The 54 new hermetic tests cover the sweep service against fakes, the alert and detection routes, five new RBAC matrix cases and the CLI |
| E-47 | Push 91bf8d6 (Chunk 9): GitHub Actions `ci` run **33968400301**, `security` run **33968400277**, SonarCloud check | ✅ all five `ci` jobs green with no annotation — `backend` 50s (658 hermetic tests), `migrations` 67s (47 database tests incl. revision 0003 and the end-to-end sweep), `stack` 106s (migrate to `0003_detection_tables`, the version probe expects it, HTTP ingest, then a queued sweep with the worker's `D-001 success` run, `/alerts` and `/detections/rules` readable); `security` green; SonarCloud **Quality Gate passed** |
| E-48 | 2026-09-05, Chunk 10: `ruff check` (backend, `tools/`), `ruff format --check`, `mypy` (78 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85`, native database suite, `python3 tools/gen_labelled_fixtures.py` into a temporary directory | clean; both contracts kept; `686 passed, 47 skipped`, coverage 95 % (`auth_burst.py` 96 %, `dns_anomaly.py` 97 %); `47 passed` natively with every sweep now recording three runs. The 28 new tests cover D-002 (case-insensitive indicator matching on signature and category, the densest-span helper, inclusive count, the steady-probe guard that a 600-second burst window defeats, per-source aggregation of targets, signature ids and categories, purity and source ordering) and D-003 (base-domain and label helpers, entropy, the allow-list, the tunnel signal against a 49-name near miss, a low-entropy 150-host organisation and an allow-listed CDN, the NXDOMAIN signal against dilution and a 49-answer near miss, the long-label signal against an allow-listed suffix, three signals raising confidence to 1.0 with answers attributed to the client, volume alone never firing); all thirteen new labelled cases pass (three positives fire on the labelled client, seven negatives stay silent) and the regenerated fixtures are byte-identical |
| E-49 | Push 3aaa6d5 (Chunk 10): GitHub Actions `ci` run **33970419849**, `security` run **33970419774**, SonarCloud check | ✅ all five `ci` jobs green with no annotation — `backend` 49s (686 hermetic tests), `migrations` 112s (47 database tests; every sweep records three runs), `stack` 98s (the queued sweep now records D-001, D-002 and D-003 over the corpus); `security` green; SonarCloud **Quality Gate passed** |
| E-50 | 2026-09-05, Chunk 11: `ruff check` (backend, `tools/`), `ruff format --check`, `mypy` (83 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85`, native database suite, `make test-db`, the E-46 stack demo extended with `make recompute-baselines`, `GET /api/v1/detections/baselines` and `POST /api/v1/detections/baselines/recompute` | clean; both contracts kept; `728 passed, 50 skipped`, coverage 94 % (`beaconing.py` and `volume_anomaly.py` 95 %, `baseline_service.py` 98 %); `50 passed` natively: the baseline store upserts by asset, metric and window; the hourly aggregation groups external-bound bytes per hour and ignores internal destinations; the job writes one row per asset with history; D-005 abstains against a one-hour baseline and fires at severity 5 once a mature baseline is upserted. The 42 new hermetic tests cover D-004 (interval statistics, the explicit internal list that keeps documentation ranges external, the jitter and minimum-interval guards, exclusion by internal destination, port, protocol and operator CIDR, the most regular destination leading with noise ignored, purity), D-005 (the largest of three bounds, the reproducible ratio, abstention without a baseline, under `min_samples` or for another metric, inbound and internal bytes ignored, confidence by sample count), the baseline statistics, the job (one row per asset with history, upsert not append, internal traffic ignored), the sweep mapping asset rows to window addresses, the baselines routes, two RBAC cases and the CLI; all fourteen new labelled cases pass and the fixtures are byte-identical to their generator. `make test-db`: `50 passed`. Stack: `alembic` at `0003_detection_tables`; the queued sweep recorded five `success` runs over the corpus (D-004 and D-005 included, no alert from the benign corpus); `make recompute-baselines` summarised 12 of the 14 seeded assets (the two without outbound flows get no row) with `sample_count 2` for the two-hour corpus; `GET /detections/baselines` listed the 12 rows; the HTTP recompute request answered `202`; `make down` |
| E-51 | Push 0294f64 (Chunk 11): GitHub Actions `ci` run **33972105660**, `security` run **33972105668**, SonarCloud check | ✅ all five `ci` jobs green with no annotation — `backend` 41s (728 hermetic tests), `migrations` 84s (50 database tests incl. the baseline job end to end), `stack` 102s (the queued sweep records all five rules); `security` green; SonarCloud **Quality Gate passed** |
| E-52 | 2026-09-05, Chunk 12: `ruff check`, `ruff format --check`, `mypy` (88 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet`, native database suite, `make test-db`, `make eval`, `docker compose config` | clean; both contracts kept; `757 passed, 51 deselected`, coverage 94 % (`evaluation.py` 99 %, `evaluation_service.py` 95 %, `schedule.py` 100 %); `51 passed` natively and under `make test-db`: `batch_span` answers a batch's own event-time range and `None` for an unknown batch; the stub-broker actor run now finds exactly one `run_detectors` message on the detection queue afterwards, over `2026-09-01T00:00:00+00:00`–`02:00:00+00:00`, the hour-aligned span of the corpus. The 29 new hermetic tests cover the cron lines and the cadence validator, the scheduled interval on the cadence grid, hour-aligned post-ingest intervals split at 24 h, `sweep_batch` against the fake store, the periodiq middleware on the broker with the configured skip delay, both periodic actors registered on the detection queue with their cron specs (a stub broker, `SWEEP_CADENCE_MINUTES=15`), the sync route queuing one sweep and auditing `sweeps_queued` (and none with `POST_INGEST_SWEEP=false`), the evaluation verdicts (exact entity, extra entities, severity floor, negative cases), the metrics arithmetic with undefined ratios, the benign scoring on each rule's grid, the marker replacement, the CLI finding the checkout above its working directory, taking no paths, writing the block without settings and exiting 1 outside a checkout, and the pin: `docs/evaluation.md` §8 equals what the harness renders (34 cases, 15 positive and 19 negative, all met; 2 000 benign events, 0 alerts for every rule, D-005 abstaining). The compose policy suite holds `scheduler` to no port, full capability drop, no volume, Redis-only dependency and a self-excluding `pgrep` probe. Stack (`make up` rebuilt the image with periodiq): all six services healthy; the scheduler logged `Registered periodic actors` with `*/10 * * * * aegisnet.workers.schedule:scheduled_sweep@detection` and `0 2 * * * … nightly_baselines@detection`; the worker registered six actors; `make migrate` to `0003_detection_tables`; a `mode=sync` upload of three fresh lines answered `200` and its `ingest.batch_created` audit entry carried `sweeps_queued: 1`, after which five `success` runs over `2026-09-01T00:00`–`01:00` (3 events) appeared without anyone asking; the async upload of the whole corpus (`stored 1997, duplicate 3`) queued a second sweep, five more `success` runs over `00:00`–`02:00` with `events_examined 2000` and `alerts_created 0`; `GET /alerts` empty as expected for the benign corpus; `make down` |
| E-53 | Pushes 41ecd32, ef1e992, 13e4b9e (Chunk 12): GitHub Actions `ci` run **33975644690**, `security` run **33975644708**, SonarCloud check on 13e4b9e | ✅ all five `ci` jobs green — `backend` 61s (759 hermetic tests), `migrations` 83s (51 database tests incl. the post-ingest sweep message on the detection queue), `stack` 131s (six services healthy; the completed batch queued its own sweep and D-005 recorded a run without a request; the scheduler logged both periodic actors); `security` green; SonarCloud **Quality Gate passed**. Two corrections on the way: (1) the stack job on 41ecd32 failed on its own shell, not the stack — `docker compose logs \| grep -q` under `pipefail` exits 255 once grep closes the pipe; ef1e992 reads the logs into files first. (2) SonarCloud rated the new code **C on security** on 41ecd32/ef1e992; a bisection by `sonar.exclusions` on `[skip ci]` commits (7443376 excluding the harness files: pass; bb9d481 excluding `cli.py` alone: pass) placed it in `cli.py`, and a local SonarQube Community scan of the same tree reported 0 vulnerabilities and 0 hotspots, so it was the taint flow from `eval-detectors`' path arguments into file reads and a write. 13e4b9e removes the arguments: the command resolves the cases, the corpus and the document at fixed names under the checkout it finds above its working directory (the dataset-import rule: no path is ever accepted), `--no-write` leaves the document alone, the tests build a miniature checkout, and the exclusions are back to the baseline |
| E-54 | 2026-09-06, Chunk 13 (the isolated Suricata lab): `ruff check`, `ruff format --check`, `mypy`, `lint-imports`, `ENV=test uv run pytest --cov=aegisnet`, native database suite, `make test-db`, `make lab-preflight`, `make lab-capture`, `make lab-sanitize`, `make eval-lab`, `make eval`, `docker compose config` on all three manifests | clean; both contracts kept. **Pre-flight:** `aegisnet_lab` reports `Internal=true`, subnet `203.0.113.0/24`, `Gateway=invalid IP`; `infra/lab/preflight.py`, run inside the target container, reports `default routes: none` and `answered … : nothing` for the first address of its own subnet and for a public address; the same script on a network without `com.docker.network.bridge.inhibit_ipv4` fails and lists six ports that replied, which is how the check was shown able to fail. Only lab containers are attached. **Capture:** Suricata 8.0.6 logged `Setting engine mode to IDS mode by default` and `Engine started` with one capture thread and a single capability (`NET_RAW`); the six scenarios ran (20 benign requests, 12 refused logins, 40 refused connections, 12 beacons at 5 s to port 9443, 4 MiB each way, 90 DNS queries) and the sensor wrote 471 EVE records. **Sanitiser:** 463 published, 8 sensor records dropped (and one run refused outright until the `ts_gap` stream marker was classified, which is the allowlist doing its job); the excerpt is registered as dataset `lab-capture-01` (sha256 `fc213715de2d…`). **T3 run** (fresh database): 463 of 463 records stored with 0 rejects, a re-import reported 463 duplicates, and the sweep over `17:00Z – 18:00Z` recorded five `success` runs and two alerts — D-001 on `203.0.113.20` (43 distinct ports, severity 3) and D-002 on `203.0.113.10` (12 failures in a 120 s span, confidence 1.0). D-003, D-004 and D-005 stayed silent; the first two are the defects L-F1 and L-F2 recorded in `docs/evaluation.md` §9, the third is D-005 correctly abstaining without 24 hours of baseline. **Suite:** `870 passed, 51 deselected`, coverage 94 %; `51 passed` natively and under `make test-db`. The 111 new hermetic tests are the lab policy suite (internal network, opt-in profile, no port or host namespace, the one `NET_RAW` exception, the digest-pinned image, no credential or datastore reach, nine inline markers absent, alert-only rules in the reserved sid range, no address or domain outside documentation space, no offensive tooling, the generator's single destination, the capture volume, the ignore rules), the sanitiser (content stripping at depth, the header-, name- and file-bearing fields a foreign sensor configuration can add, refusal on a public address and on a real name hidden inside a URL, a referer, an SNI or a DNS record, string bounds, the limit, the manifest's sweep window, both CLI paths, and a check of the committed excerpt) the fidelity suite over the real capture (every record normalises, D-001 and D-002 fire, and L-F1, L-F2 and L-F3 are pinned as facts), and the corpus integrity tests extended to the lab dataset (registry sha, manifest counts, every line normalising, documentation addresses only) |
| E-55 | Pushes e1723d4 … cdf2adf (Chunk 13): GitHub Actions `ci` run **33987021977**, `security` run **33987021915**, SonarCloud check on cdf2adf | ✅ all five `ci` jobs green — `backend` (870 hermetic tests), `migrations` (51 database tests), `manifests` (now parsing the lab manifest too), `stack` (six services healthy, the post-ingest sweep, the scheduler's actors); `security` green; SonarCloud **Quality Gate passed**. Four corrections on the way, each found by a check rather than by luck: (1) gitleaks read `api_key=sk-live-…` in the test that proves such a parameter is refused and called it a leak — the test now builds its value from an expression; (2) SonarCloud rated the new code **C on security** through four bisections by `sonar.exclusions` (24bb343, 99b706f, 5559506, 59be0f2) and a local SonarQube scan that named `python:S5332` on the lab target's `serve_forever`, marked `NOSONAR` with the reason written above it (the lab must speak clear-text HTTP or there are no http records to read); the second, invisible to the local scanner, was the generator's `--wait-for` path argument, removed the same way `eval-detectors` and the sanitiser take no paths; (3) an adversarial review of the chunk found that `internal: true` leaves the subnet's first address on the host side of the bridge and reachable — reproduced against rpcbind on `203.0.113.1:111` from inside the lab — closed with `com.docker.network.bridge.inhibit_ipv4` and now proved by `infra/lab/preflight.py` from inside a container, a check verified to fail on a network without the option; (4) the same review found the sanitiser never inspected a string inside a list, so an address in a DNS answer would have been published — the checker now reaches every scalar, refuses an unclassified key, and judges a `--check` file as it sits on disk rather than a stripped copy of it |
| E-56 | 2026-09-06, Chunk 14 (the two defects the lab found): `ruff check`, `ruff format --check`, `mypy`, `lint-imports`, `ENV=test uv run pytest`, `make test-db`, `make gen-fixtures`, `make gen-synthetic`, `make eval`, `make eval-lab` on a fresh database | clean; both contracts kept; `898 passed, 51 deselected` and `51 passed` on PostgreSQL. **The fixes:** a flow event is filed under `flow.start` (every other type unchanged), and a DNS record's direction comes from its own `type` rather than from carrying an `rcode`. **The generators** had the same assumptions in them and were corrected, so the corpus (generator version 2, sha256 `0b1f39b59ceb…`) and all 34 labelled fixtures were regenerated; both generators also write EVE DNS v3 now, so T1 and T2 exercise the shape that broke D-003 instead of leaving it to the lab alone; the normalised event times did not move and the §8 table is byte-identical, which is the finding in itself — the T1/T2 numbers were the same before and after a change that took two rules from blind to working. **On the committed real capture**, run through the stack on a clean database: 463 of 463 stored, and the sweep recorded **four alerts from four rules** (D-001 `203.0.113.20` signal 0.72, D-002 `203.0.113.10` 0.40, **D-003 `203.0.113.20` 0.50**, **D-004 `203.0.113.20` 0.40**), against two before; D-005 abstains for want of 24 hourly baseline samples. 28 new hermetic tests: the semantics on hand-built lines (flow start absent, empty, garbage, naive and malformed; the freshness window checked against both instants; the event hash unchanged by the change; v2 query/answer, v3 request/response, the no-type fallback and an unfamiliar type), the timestamp parser itself, and the invariants the generators must now hold — a flow starts before it is emitted, in the corpus and in all 34 cases, and the corpus carries both EVE DNS shapes, the two lab fidelity tests flipped from recording the defects to holding the fixes, and one database expectation corrected — the benign fixture's flow began at 09:59:30 and was emitted at 10:00:04, so it is now counted in the hour it began in |
| E-57 | Pushes 6ff7d3c … 1b810fd (Chunk 14): GitHub Actions `ci` run **33990219967**, `security` run **33990219958**, SonarCloud check on 1b810fd | ✅ all five `ci` jobs green — `backend` (899 hermetic tests), `migrations` (51 database tests), `stack` (six services, the post-ingest sweep, the scheduler's actors), `manifests`, `frontend`; `security` green; SonarCloud **Quality Gate passed**. Two corrections on the way, both of them the same lesson a third and fourth time: SonarCloud rated the new code **C on security**, a bisection by `sonar.exclusions` (d72e96f, then narrowing to the fixture generator alone) placed it in `tools/`, and a local SonarQube scan reported nothing — so, taint. First the two generators' `--out` arguments went, the way `eval-detectors` and the sanitiser had already given up theirs; the tests pass a destination to `write_corpus` and `write_all` instead, which is a parameter rather than a command line. That was not the whole of it: the fixture generator still built a path by joining several components onto a root discovered at runtime, and now validates every segment and proves the result stayed under the root, the rule `adapters/files/registry.py` already applied to dataset paths. The committed fixtures are byte-identical after both changes. An adversarial review of the chunk also found a comment naming a helper that does not exist and an over-broad claim in ADR-022 about deduplication, both corrected |
| E-58 | 2026-09-06, Chunk 15 (correlation and incidents): `ruff check`, `ruff format --check`, `mypy` (93 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet`, `make test-db`, `make test-correlation` | clean; both contracts kept; `941 passed, 60 deselected`, coverage 93 % (`domain/correlation.py` 92 %, `domain/incidents.py` 95 %, `services/correlation_service.py` 98 %); `60 passed` on PostgreSQL 16. Revision **0004** applies and downgrades cleanly: nineteen tables, eighteen enum types, `compare_metadata` clean, the grants matrix unchanged in shape (SELECT/INSERT/UPDATE on the four new tables, USAGE on `incident_case_seq`, DELETE still only on `asset_networks`), and the version route now reports `0004_incident_tables`. The 42 new tests are the grouping policy as scenarios (a four-rule multi-stage attack on one host is one escalated case; two hosts are two cases; `src_ip` and `dest_ip` for the same address are different keys; a gap longer than the join window starts a new case while continuing activity keeps one open; a case stops at twenty-four hours; shuffling the input changes nothing), the workflow table (every open state can close, a closed case reopens only into `investigating`, no status is a dead end or lists itself), the service against fakes (**idempotence** — a second run over the same window opens nothing, links nothing and adds no timeline line; a **closed case is never extended** and the new case's timeline names it, but only when the closure is recent; correlated alerts are not reconsidered; an alert already in a case stays there), and the store against PostgreSQL (the case-number sequence hands out `AEG-2026-0001`, `-0002`, `-0003`; the UNIQUE on `alert_id` means a second case cannot steal an alert; the timeline UNIQUE means a re-run says nothing new; the window grows and never shrinks; the partial index returns the open case and never a closed one; and the database refuses a closed status without its `closed_at`). **On the stack, over the committed lab capture:** `make migrate` applied 0004, the sweep produced its four alerts, and `make correlate` over the capture's hour opened exactly two cases — `AEG-2026-0001`, "3 rules on 203.0.113.20: D-001, D-003, D-004", severity 4 escalated from a member maximum of 3, with three `alert_fired` lines in its timeline, and `AEG-2026-0002` for the auth-failure alert whose entity is the target rather than the generator. Two entities, two cases, no guessing. A second `make correlate` over the same hour considered 0 alerts and changed nothing |
| E-59 | Push ec9558e (Chunk 15): GitHub Actions `ci` run **33993266446**, `security` run **33993266011**, SonarCloud check | ✅ all five `ci` jobs green on the first push — `backend` (941 hermetic tests), `migrations` (60 database tests, revision 0004 applying and downgrading against PostgreSQL 16), `stack` (six services healthy, the version route reporting `0004_incident_tables`), `manifests`, `frontend`; `security` green; SonarCloud **Quality Gate passed** with no bisection needed, the first chunk since the path-taint rule was learned that introduced no new command-line path |
| E-60 | 2026-09-06, Chunk 16: `ruff check` (backend, `tools/`), `ruff format --check`, `mypy` (95 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85`, `make test-db`, `make test-correlation` | clean; both contracts kept; **`1014 passed, 72 skipped`, coverage 94 %**; **`72 passed`** against PostgreSQL 16 in Compose, up from 60 — the new database tests prove what only a real database can: the compare-and-set writes nothing when it loses the race, `ck_incidents_closed_at_matches_status` is satisfied closing *and* reopening, a case takes many `status_change` rows because PostgreSQL counts NULLs as distinct in `uq_incident_timeline_alert_entry`, and correlation's `ON CONFLICT DO NOTHING` still no-ops after the append path was split. No new migration: revision 0004 already carried every column, constraint, index and grant, so the head stays `0004_incident_tables`. Coverage rose because `adapters/db/incident_store.py` joined its six sibling SQL stores in the hermetic gate's `omit` list, where Chunk 15 had left it out. 72 new tests: 23 on the service (every refusal path, the lost race, reopening, note cleaning), 16 over HTTP (the walk to closed with the timeline asserted line by line, `409` + the audited denial, a viewer refused on both write routes, bounded pagination), 24 in the database suite, 9 on the domain's free-text rules, plus 6 RBAC matrix rows × 4 roles. The adversarial review caught two real defects before the commit, both fixed and both pinned by a database test that fails without the fix: three keyset cursors compared as Python tuples rather than SQL rows (collapsing to the timestamp alone and silently dropping rows at a tied instant — one of them shipped in Chunk 15), and `extend` lacking the closed-status guard that ADR-023's invariant needs now that closing a case is reachable |
| E-61 | Push 250fa0a (Chunk 16): GitHub Actions `ci` run **34016762508**, `security` run **34016762504**, SonarCloud check | ✅ all five `ci` jobs green on the first push — `backend` (1014 hermetic tests), `migrations` (72 database tests, revision 0004 applying and downgrading against PostgreSQL 16 with no new revision needed), `stack` (six services healthy, the corpus ingested over HTTP, the post-ingest sweep and the queued sweep both recorded), `manifests`, `frontend`; `security` green (`pip-audit`, `pnpm audit`, `gitleaks`); SonarCloud **Quality Gate passed** with no bisection. Second chunk running to clear every gate on the first push, and the second since the path-taint rule was learned that introduced no command-line path |
| E-62 | 2026-09-06, Chunk 17: `ruff check` (backend, `tools/`), `ruff format --check`, `mypy` (97 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85`, then `make down -v && make up && make migrate && make demo-scenario` on a **clean** database | clean; both contracts kept; **`1036 passed, 72 skipped`, coverage 94 %**. The M3 acceptance criterion, end to end on PostgreSQL from an empty schema: `seed-assets demo-scenario` created two assets, `import-dataset` stored **303 of 303** events with 0 rejected, `recompute-baselines --until 2026-09-05T09:00:00Z` wrote **1 baseline** from 168 history hours, the sweep over the seven-hour window examined 135 events and created **6 alerts** (D-001 ×3, D-002, D-004, D-005 — D-003 correctly silent), and `correlate` opened exactly **three** cases: **`AEG-2026-0001` “4 rules on 10.10.0.42: D-001, D-002, D-004, D-005”, four alerts, severity 5, escalated**, `AEG-2026-0002` for the unrelated host, and `AEG-2026-0003` for the *same* host's separate scan six hours later. The pure `make eval` harness scores that grouping at precision/recall 1.00 over 6 pairs, fragmentation 1.00 (3 ÷ 3) and contamination 0.00, and reaches severity 5 as well by different arithmetic. Its reconstructed baseline matches the one the database job wrote exactly — 168 samples, mean 5138022.0, stddev 629145.6, p95 6081740.0. The first draft of the scenario was dated after `EVALUATION_CLOCK` and more than half its records were refused as future timestamps by T-1.7, the guard the demo exists to exercise |
| E-63 | Push 33368f9 (Chunk 17): GitHub Actions `ci` run **34019140946**, `security` run **34019140871**, SonarCloud check | ✅ all five `ci` jobs green on the first push — `backend` (1036 hermetic tests), `migrations` (72 database tests; no new revision, the head stays `0004_incident_tables`), `stack`, `manifests`, `frontend`; `security` green (`pip-audit`, `pnpm audit`, `gitleaks`); SonarCloud **Quality Gate passed** with no bisection. Third chunk running to clear every gate on the first push. **Milestone 3 is complete**: all five acceptance criteria in `docs/delivery-plan.md` M3 are ticked with named evidence |
| E-64 | 2026-09-06, Chunk 18: `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build` in `frontend/`, then `docker compose up -d --build web` and the dashboard driven over HTTP against the live stack | clean; **32 vitest tests** over the zod schemas, the API client, the session and the redirect allow-list; `next build` produced five dynamic routes and the middleware bundle. Against the running stack: an unauthenticated `GET /` answered **307 → /login**; a real analyst session rendered all three cases from the Chunk 17 scenario — `AEG-2026-0001` "4 rules on 10.10.0.42: D-001, D-002, D-004, D-005" at severity **5 critical**, `AEG-2026-0002` at 3, `AEG-2026-0003` at 4 — with the masthead reading `analyst · analyst`; `?severity_min=5` narrowed to the one critical case; `?status=triaging` showed the empty state; `?status=nonsense&severity_min=99` was ignored rather than erroring (HTTP 200); a viewer session rendered the same three cases with the masthead reading `viewer`. The ESLint ban on `dangerouslySetInnerHTML` was proven by adding a component that trips it (error raised, naming T-1.3) and then removing it. The authenticated pages were exercised with cookies minted from the API rather than by typing a password into a browser field |
| E-65 | Push af06ab3 (Chunk 18, after 7ac5e92 and 8fd24ba): GitHub Actions `ci`, `security`, SonarCloud | ✅ all five `ci` jobs green — `frontend` now runs `tsc`, `eslint`, `vitest` and `next build`; `backend`, `migrations`, `stack`, `manifests` unchanged; `security` green; SonarCloud **Quality Gate passed**. It did **not** pass first time: 7ac5e92 failed on *B Security Rating on New Code*, and so did 8fd24ba, which fixed the two flows I had guessed at. Guessing stopped there. Four `[skip ci]` commits bisected `sonar.exclusions` — the whole of `frontend/src` (passed, so the finding was in the new frontend), then `frontend/src/app/**` (still failed), then everything but `frontend/src/lib` (passed), then everything but `frontend/src/middleware.ts` (**passed**) — which located it in exactly one file. Two flows there handled attacker-controlled input by string concatenation: the login redirect was built by joining `request.url`, and the onward cookie header spliced the browser's raw `Cookie` header into an outgoing one. Both are now assembled from parsed parts — a cloned URL whose search is cleared and whose parameters this file sets, and `request.cookies.getAll()` name/value pairs with this app's two replaced. The exclusions were removed in the same commit; the analysis scope is whole. Re-verified against the stack afterwards: a fresh analyst session renders all three cases, and a request carrying **only** the session cookie was rotated by the middleware, rendered, and came back with both cookies rewritten |
| E-66 | 2026-09-06, Chunk 19: `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, then the case view driven over HTTP against the live stack | clean; **84 vitest tests** (up from 32), of which 30 are `SafeMarkdown` against 15 hostile inputs and 12 are the case-view server actions. Against the running stack, `AEG-2026-0001`: the page rendered the header, the four linked alerts with their own severities, the timeline, and a status control offering exactly the five moves the API listed in `allowed_transitions` (triaging, investigating, and the three closed statuses) — the workflow is never recomputed in the browser. A note was written through the API carrying `<script>alert(document.cookie)</script>`, `<img src=x onerror=alert(1)>`, a markdown link to `evil.test` and a fenced `<svg/onload=…>`; the rendered note contained **only** `br, code, div, li, p, pre, span, strong, time` — no `script`, `img`, `svg` or `a` — with the script text visibly escaped and the markdown (bold, code, fence) honoured. Signed in as a **viewer**: the case, its notes and its timeline all rendered, with no status control and no note form, and an explanation of why; forging the requests anyway answered **403** from the API for both the status change and the note. The analyst's write path through the dashboard's own server actions is unit-tested rather than driven in a browser; Playwright covers it in Chunk 20 |
| E-67 | Push 8fc5585 (Chunk 19): GitHub Actions `ci`, `security`, SonarCloud | ✅ **all nine checks green on the first push** — `ci`'s five jobs (`frontend` running `tsc`, `eslint`, 84 vitest tests and `next build`; `backend` 1036 tests; `migrations` 72; `stack`; `manifests`), `security`'s three (`pip-audit`, `pnpm audit`, `gitleaks`), and SonarCloud **Quality Gate passed** with no bisection — the first frontend chunk to clear the security rating without one, after Chunk 18 needed four `[skip ci]` commits to find its finding |
| E-68 | 2026-09-06, Chunk 20: `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, then `pnpm e2e` and `pnpm e2e:shots` against the running stack | clean; **99 vitest tests** (up from 85 — the new ones compute WCAG contrast from `globals.css` for both themes) and **14 Playwright tests passing in Chromium against the live stack**: a note carrying `<script>`, an `onerror` image, a `javascript:` link, a tracking image and `<svg/onload>` rendered visibly as text with no dialog, no `window.__pwned`, and no `script`/`img`/`svg`/`a`/`iframe` in the notes list; a viewer read a case with no status control and no note form and was answered **403** by the API when both requests were forged; the queue, a case and the status control were reached and operated from the keyboard, with a visible focus ring. Three screenshots written into `docs/screenshots/`. **The browser suite earned its place immediately**: `actions.ts` exported a constant beside its two server actions, which Next refuses from a `"use server"` module at *run* time — `tsc`, ESLint, 84 unit tests and `next build` had all passed while the note form was broken, and the first browser test to submit a note found it. Two test-design corrections along the way: signing in per test spent the API's five-logins-per-fifteen-minutes budget (now one `storageState` sign-in per role, one file each), and the first XSS assertions checked for absent substrings rather than for emitted elements |
| E-69 | Push 6c90bdc (Chunk 20): GitHub Actions `ci`, `security`, SonarCloud | ✅ **all ten checks green on the first push**, including the new **`e2e` job — Playwright in Chromium against a stack CI built itself**: it started the six services, applied the migrations, ran `make demo-scenario` so there were real cases to open, minted an analyst and a viewer with masked random passwords, installed Chromium and ran all 14 tests. Also green: `backend` (1036 tests), `frontend` (`tsc`, ESLint, 99 vitest tests, `next build`), `migrations` (72), `stack`, `manifests`, `pip-audit`, `pnpm audit`, `gitleaks`, and SonarCloud **Quality Gate passed** with no bisection. **Milestone 4 is complete**: all five acceptance criteria in `docs/delivery-plan.md` M4 are ticked with named evidence |
| E-70 | 2026-09-06, Chunk 21: `ruff check` (backend, `tools/`), `ruff format --check`, `mypy` (101 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85` | clean; both contracts kept; **`1072 passed, 72 skipped`, coverage 94 %**. 36 of the new tests are the canary suite `docs/delivery-plan.md` M5 asks for, asserting against the **serialised body** rather than the object: every field poisoned with canary emails, `AKIA…` keys, private-key blocks, JWTs, bearer tokens, `password=` assignments, provider tokens and base64 blobs, and none of them — nor their first 24 characters — appear in the bytes that would be sent. Also pinned: addresses and hostnames leave only as tokens, an unclassified evidence key is dropped *and recorded*, the numbers a brief reasons from survive (a redactor that dropped everything would pass the rest), two builds of one case are byte-identical, the 24 kB cap holds with truncation flagged, and five prompt-injection strings planted in a DNS name reach the packet as `domain-1`. **The suite failed on its first run and found a real leak**: `correlation_service` writes timeline summaries quoting the entity (`D-001 fired on src_ip 10.10.0.42`), which the credential denylist had no reason to object to; the pseudonymiser now substitutes addresses inside sentences too. No outbound request exists in the repository |
| E-71 | Push 8d0c7c0 then 6470cb6 (Chunk 21): GitHub Actions `ci`, `security`, SonarCloud | ✅ all ten checks green on 6470cb6 — `backend` (1072 tests), `frontend`, `e2e` (14 Playwright), `migrations`, `stack`, `manifests`, `pip-audit`, `pnpm audit`, `gitleaks`, SonarCloud **Quality Gate passed**. The first push failed **`gitleaks`**, correctly: the canary suite had a JWT and a GitHub PAT written out as literals, and a test whose purpose is proving such strings never leave should not put a scannable one in the repository (the same lesson as Chunk 6's HS256 key, E-38). Both are now joined at runtime, so the shape the scanner under test must recognise is unchanged — `test_the_scanner_recognises_every_canary` still passes — while no literal remains for the scanner watching the repository. The values were always fake; the point is not training anybody to ignore the alarm |
| E-72 | 2026-09-06, Chunk 22: `ruff check` (backend, `tools/`), `ruff format --check`, `mypy` (106 files, strict on `domain/`), `lint-imports`, `ENV=test uv run pytest --cov=aegisnet --cov-fail-under=85` | clean; both contracts kept; **`1127 passed, 72 skipped`, coverage 94 %**; 55 new tests, all in `tests/security/`. Every M5 failure mode the plan names now has a named reason and a test: timeout and transport error (recorded by exception *type*, because an httpx error carries the request and the request carries the key), 429 and 5xx retried with jittered backoff, 401/403/404 not retried, `malformed_json`, `malformed_brief` when the model answers in prose, `response_too_large` checked before parsing, `schema_rejected`, and `safety_rejected`. Also pinned: the feature is off by default and a key alone does nothing; only the packet is sent, wrapped in a delimiter, to exactly one https host that cannot be redirected or downgraded; the key appears in no log record and the scrubber would catch it anyway; an unchanged case is answered from cache without reaching the network; the daily budget is a hard stop that resets on a new UTC day; an uncited external claim is admitted and flagged rather than refused; and `InvestigationBrief` has no field for a severity, a status or a verdict. **A design bug was found by the tests**: the safety filter was a pydantic `model_validator`, and pydantic converts a `ValueError` raised there into a `ValidationError` — so "the model recommended attacking something" was indistinguishable from "a field was too long" and `safety_rejected` could never have been recorded. Safety is now its own step after validation. **No outbound call has been made**; the transport is a mock and every response is a committed fixture |

## Milestone tracker

| Milestone | Status | Evidence | Notes |
|---|---|---|---|
| M0 Planning | ✅ Complete | This doc set | PRD, architecture, threat model, data model, M1 API, delivery plan, evaluation plan |
| M1 Foundation / ingest / normalize / assets | ✅ Complete — Chunks 1–7 | E-1 – E-8, E-18 – E-43 | — |
| M2 Five detectors + labelled fixtures | ✅ Complete — Chunks 8–13, with Chunk 14 as its follow-up | E-44 – E-57 | Every acceptance criterion in `docs/delivery-plan.md` is ticked with evidence. The lab's two findings (L-F1, L-F2) were defects rather than unmet criteria, and Chunk 14 fixed both (ADR-022); four of the five rules now fire on the committed real capture |
| M3 Correlation / incidents / workflow | 🟡 In progress — Chunk 15 done | E-58, E-59 | Next: Chunk 16, the incidents API with audited transitions and the RBAC matrix, then the multi-stage demo scenario and the correlation metrics |
| M4 Analyst dashboard | ⬜ Not started | — | Blocked on M3 |
| M5 Perplexity brief + Markdown export | ⬜ Not started | — | Blocked on M4 (safe renderer first) |
| M6 Hardening / evaluation / release | ⬜ Not started | — | Gate for `v1.0.0` |

## Milestone 1 chunk tracker

| Chunk | Contents | Status |
|---|---|---|
| 1 | Skeleton, Compose, config, logging, health, worker topology, web placeholder, tests, CI | ✅ Locally verified |
| 2 | Alembic baseline migration, ORM models, DB grants incl. `audit_log` | ✅ Verified locally (native E-18, Compose and stack paths E-20) and in CI (E-21) |
| 3 | EVE domain: schema, sanitizer, normalizer, `event_hash`, synthetic generator, registry | ✅ Verified locally (E-22) and in CI (E-23) |
| 4 | Ingest service, first Dramatiq actor, rejects, idempotency | ✅ Verified locally (E-24 – E-27) and in CI (E-28) |
| 5 | Assets API, events read API | ✅ Services, stores and CLI verified locally (E-29 – E-32) and in CI (E-33); HTTP routes with Chunk 6 |
| 6 | Auth, RBAC, audit, rate limits, `SECURITY.md` | ✅ Verified locally (E-34 – E-37) and in CI (E-38) |
| 7 | Docs update at the M1 gate with CI evidence | ✅ Verified locally (E-42) and in CI (E-43) |

## Milestone 2 chunk tracker

| Chunk | Contents | Status |
|---|---|---|
| 8 | Detector contract (bounded windows, bounded evidence, dedup keys, samples), severity formula, registry, D-001 port scan, labelled fixtures and their generator, `docs/detection-rules.md` | ✅ Verified locally (E-44) and in CI (E-45) |
| 9 | Detection schema (revision 0003: `detection_rules`, `detector_runs`, `alerts`, `alert_events`, `alert_assets`, `asset_baselines`), alert store with dedup, window loader, sweep service with per-rule failure isolation, `run_detectors` actor, `/api/v1/alerts` read API, `make run-detectors` | ✅ Verified locally (E-46) and in CI (E-47) |
| 10 | D-002 auth-failure burst and D-003 DNS anomaly with fixtures | ✅ Verified locally (E-48) and in CI (E-49) |
| 11 | D-004 beaconing (known-periodic allow-list) and D-005 outbound volume with the baseline recompute job | ✅ Verified locally (E-50) and in CI (E-51) |
| 12 | periodiq schedule (ADR-010 revisited), post-ingest sweep, `make eval` and the first metrics table | ✅ Verified locally (E-52) and in CI (E-53) |
| 13 | The isolated Suricata lab (ADR-009 revisited) and the documents at the M2 gate | ✅ Verified locally (E-54) and in CI (E-55) |
| 14 | The two defects the lab found: a flow event's time and a DNS record's direction (ADR-022) | ✅ Verified locally (E-56) and in CI (E-57) |

## Milestone 3 chunk tracker

| Chunk | Scope | State |
|---|---|---|
| 15 | The correlation engine, the four incident tables, the workflow state machine and the correlation CLI (ADR-023) | ✅ Verified locally (E-58) and in CI (E-59) |
| 16 | The incidents API: list, detail, timeline, status transitions with audit, notes, and the RBAC matrix (ADR-024) | ✅ Verified locally (E-60) and in CI (E-61) |
| 17 | `make demo-scenario`, the multi-stage correlation scenario, and the correlation metrics in `docs/evaluation.md` §8 (ADR-025) | ✅ Verified locally (E-62) and in CI (E-63) |

## Milestone 4 chunk tracker

| Chunk | Scope | State |
|---|---|---|
| 18 | The dashboard's foundation: sign-in and the session model, the typed API boundary, the app shell and the incident queue (ADR-026) | ✅ Verified locally (E-64) and in CI (E-65) |
| 19 | The case view: alerts, the timeline, the status control and notes, with `SafeMarkdown` (ADR-027) | ✅ Verified locally (E-66) and in CI (E-67) |
| 20 | Asset screens, the audit viewer, Playwright including the stored-XSS fixture, committed screenshots, keyboard and contrast checks (ADR-028) | ✅ Verified locally (E-68) and in CI (E-69) |

## Milestone 5 chunk tracker

| Chunk | Scope | State |
|---|---|---|
| 21 | The redaction layer and the evidence packet: allow-list serialisation, pseudonymisation, the denylist scanner and the canary suite (ADR-029) | ✅ Verified locally (E-70) and in CI (E-71) |
| 22 | The hardened Perplexity client, the `InvestigationBrief` schema, citation resolution and the safety filter (ADR-030) | ✅ Verified locally (E-72); CI (E-73) pending. Offline mode moves to Chunk 23, where there is a route to serve it |
| 23 | Revision 0005 (`investigation_briefs`, `brief_citations`), the brief routes, the worker actor and `make brief` | ⬜ |
| 24 | The deterministic `report.md` export and the dashboard's brief panel with its citation list and `UNVERIFIED` tags | ⬜ |

## Definition-of-Done checklist (v1.0.0)

- [ ] `docker compose up --build` starts the full local environment — *the Milestone 1 stack does, on every CI run (E-38); the detectors, scheduler and dashboard that complete "full" arrive in M2 – M4*
- [x] Documented safe sample dataset ingests via one command — `make demo-ingest` (Chunk 4; E-27: second run adds zero events)
- [x] All five detectors have labelled positive **and** negative test cases — 34 labelled cases, 3 positive and 3 – 4 negative per rule, pinned to their generator (Chunk 11; E-49)
- [x] Unit + integration tests run in GitHub Actions — the `ci` workflow: hermetic suite with the coverage gate, the database suite against PostgreSQL 16, and the Compose stack (E-38, E-43)
- [ ] A reviewer reproduces the main demo from the README alone — *the CI `stack` job reproduces README steps 1 – 3 plus a login and an HTTP ingest on a clean runner (E-38); a human walk-through is still owed at M6*
- [ ] Architecture, threat model, evaluation results, limitations, screenshots, demo script all exist — *architecture and threat model exist and were reviewed at the M1 and M2 gates (E-42, E-54); evaluation results exist for T1, T2 and a first T3 lab run (`docs/evaluation.md` §8 and §9), with limitations stated in both; screenshots and the demo script are M4/M6*
- [x] Auth, RBAC, audit logging, rate limiting implemented and tested — Chunk 6 (E-34 – E-37, `SECURITY.md`); M6 completes what `SECURITY.md` lists as missing
- [ ] Redaction canary suite proves no forbidden data reaches Perplexity
- [ ] `docs/RELEASE_CHECKLIST.md` complete and signed off

## Documents

| Doc | State |
|---|---|
| `README.md` | ✅ Reflects Milestone 1 (Chunk 7): architecture diagrams, security model, quickstart with auth, roadmap |
| `ARCHITECTURE.md` | ✅ Target design with an implementation-status section reviewed at the M1 gate (Chunk 7) |
| `THREAT_MODEL.md` | ✅ Reviewed at the M1 gate (Chunk 7); every M1 row names its test or its deferral |
| `SECURITY.md` | ✅ Chunk 6: credential model, RBAC matrix, audit actions, rate limits, disclosure |
| `docs/PRD.md` | ✅ v0.1 |
| `docs/repo-structure.md` | ✅ v0.1 |
| `docs/data-model.md` | ✅ v0.1 |
| `docs/api-milestone-1.md` | ✅ v0.1 |
| `docs/delivery-plan.md` | ✅ v0.1; the M1 acceptance criteria are ticked with evidence rows (Chunk 7) |
| `docs/evaluation.md` | ✅ Plan; §8 holds the `make eval` table from T1 (34 labelled cases) and T2 (the benign corpus), pinned by a test (Chunk 12); §9 holds the first T3 lab run, the two defects it found and the re-run after they were fixed (Chunks 13 – 14); the L-0 – L-5 pre-flight checklist is ticked. Accuracy on real *networks* remains unmeasured |
| `samples/README.md` | ✅ Chunk 3: datasets, registry fields, how a file is imported |
| `docs/detection-rules.md` | ✅ Chunks 8 – 11: the shared contract, how a sweep runs, and the specifications, guards, hard negatives and limitations of all five rules |
| `docs/perplexity-integration.md` | ⬜ M5 |
| `docs/demo-script.md` | ⬜ M6 |
| `docs/RELEASE_CHECKLIST.md` | ⬜ M6 |

## Decisions locked in M0

| ID | Decision |
|---|---|
| D-1 | Background work: **Dramatiq + periodiq** (rationale in `ARCHITECTURE.md` §2) |
| D-2 | Modular monolith backend, strict pure `domain/` layer enforced by import-linter |
| D-3 | Deterministic heuristic detectors only in v1; no ML |
| D-4 | Suricata EVE JSON only in v1; Zeek is a documented future extension |
| D-5 | Primary demo corpus is **committed synthetic EVE**, not a downloaded dataset |
| D-6 | AI briefs are narrative-only and can never mutate detection state |
| D-7 | Events keep promoted typed columns **plus** a validated JSONB payload |
| D-8 | Auth/audit/rate-limit primitives exist from M1; M6 completes and proves them |
| D-9 | **Isolated Suricata Docker lab (`infra/lab/`) deferred to M2.** M1 is file-and-API driven only: no packet capture, no traffic generation, no live-traffic component. Rationale: sensor config, capture permissions, and topology introduce reproducibility risk that would compromise the M1 gates. **Delivered in Chunk 13 (ADR-021):** three containers on an `internal: true` network, Suricata pinned by digest in IDS mode with one capability, a sanitiser that can refuse, and two defects found in the detectors on their first contact with real sensor output (`docs/evaluation.md` §9). |
| D-10 | Scheduler and periodiq deferred to M2 (ADR-010); the M1 worker registers two actors, `import_dataset` (Chunk 4, ADR-014) and `import_upload` (Chunk 6, ADR-016). **Delivered in Chunk 12 (ADR-020):** the `scheduler` service runs periodiq against the worker entrypoint; `scheduled_sweep` every ten minutes with a one-hour lookback, `nightly_baselines` at 02:00 |

## Defects found and fixed in Chunk 1 review (2026-08-30)

| Defect | Effect | Fix |
|---|---|---|
| `RedisBroker(url=..., password=...)` ignored the password | Worker would fail with `NOAUTH` on first Redis command | Explicit authenticated `redis.Redis` client; regression test |
| Worker liveness probe `pgrep -f 'dramatiq …'` matched its own `sh -c` wrapper | Healthcheck always passed | `[d]ramatiq` pattern; policy test |
| `cap_drop: ALL` on the official `postgres`/`redis` images | Both restart-looped (`setresuid failed`); the stack had never started | `user: postgres` / `user: redis`; policy test |
| No `.gitattributes`; CRLF checkout on Windows | Bind-mounted `01_roles.sh` unusable inside the container | LF normalisation for all text files |
| README / STATUS / CHANGELOG described tests and a frontend that did not exist | Misleading status | This revision |

## Open risks being carried

| Risk | Current handling |
|---|---|
| Detector thresholds may need substantial tuning; accuracy is unknown | M2 fixtures + `docs/evaluation.md` measure it honestly before any claim is made. Narrowed twice: T1/T2 are measured (§8), the lab measured T3 qualitatively (§9), and the two defects it found are fixed (ADR-022), after which four of the five rules fire on the committed capture. What remains unknown is accuracy on a real network. D-005 is the sharpest gap: it abstains without 24 hourly baseline samples, so nothing has ever seen it judge real traffic |
| ~~L-F1: D-004 reads a flow record's emission time~~ · ~~L-F2: D-003 treats every Suricata 8 DNS record as an answer~~ | **Closed in Chunk 14 (ADR-022, E-56).** A flow event is filed under `flow.start`; a DNS record's direction comes from its own `type`. Both rules now fire on the committed real capture, and the tests that recorded the defects hold the fixes |
| The synthetic corpus can show that a rule matches its specification, not that the specification matches a sensor | The lab is the only thing that can, and it has to be re-run against every rule written from here. `docs/evaluation.md` §9 says so above its own table |
| Beaconing false positives from legitimate periodic traffic (NTP, updates) | Known-periodic destination allow-list planned in D-004 |
| Indirect prompt injection via log content | Contained, not eliminated — see `THREAT_MODEL.md` R-3 |
| Metadata egress to Perplexity is non-zero even when redacted | Opt-in per incident, off by default, offline mode supported — R-2 |
| Public-dataset licence obligations | Provenance + required citation stored per ingest batch |
| Login lockout is a fixed 15 minutes; no per-request upload timeout; proxy headers untrusted so a NAT shares one login budget | Documented in `SECURITY.md`; exponential backoff, timeouts and TLS termination are M6 work |
| A `db_data` volume initialised before Chunk 2 lacks the migrator's `CREATE ON DATABASE` grant (init scripts run once) | `make down && make up` re-initialises it; recorded in ADR-012 |
| Base images and GitHub Actions pinned by tag, not digest | Decision F-5; `make pin-digests` prints the digests. `astral-sh/setup-uv` publishes no major tags from v8 on and is already pinned to an exact release |

## Next actions

1. Chunk 23: revision 0005 (`investigation_briefs`, `brief_citations`), the brief service that joins `domain/redaction` to the client, `POST /api/v1/incidents/{id}/brief` with its reads, the worker actor, `make brief`, the offline mode that serves a committed fixture to a reviewer without a key, and moving the daily budget from memory into Redis beside the other limits.
2. Worth doing alongside M3, from Chunk 14's findings: exercise D-005 in the lab, which needs the lab to run on a schedule across a day so the baseline job has its 24 hourly samples; today it correctly abstains and is therefore the one rule the lab has never tested.
3. SonarCloud: the gate passes (E-41). To show the badge and open the API, make the SonarCloud project public (Project Settings → Visibility); nothing else depends on it.
4. Owed at M6 from the Definition of Done: a human reviewer's README walk-through, screenshots, the demo script, `docs/RELEASE_CHECKLIST.md`.
5. Keep this file, `README.md` and `THREAT_MODEL.md` updated per chunk, not afterwards.
