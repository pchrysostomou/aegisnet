# Reference URL prefixes for rules that cite one. The lab's rules cite nothing, so this
# file exists only because Suricata refuses to start when the configured path is unreadable.
config reference: url http://
